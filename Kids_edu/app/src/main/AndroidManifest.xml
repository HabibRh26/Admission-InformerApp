<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.kids_edu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Letters_Words_Activity" />
        <activity android:name=".mathDigitActivity" />
        <activity android:name=".mathTableActivity" />
        <activity android:name=".others_color_Activity" />
        <activity android:name=".others_fruit_activity" />
        <activity android:name=".others_days_Activity" />
        <activity android:name=".Others_monthActivity"></activity>
    </application>

</manifest>